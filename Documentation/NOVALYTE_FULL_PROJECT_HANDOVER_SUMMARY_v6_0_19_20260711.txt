NOVALYTE FULL PROJECT HANDOVER SUMMARY
Version: v6.0.13
Date: 2026-07-11

REQUESTED-ONLY LOCAL LAYOUT UPDATE

Completed change:
- On the client home hero, the three action buttons (Start an Order, View Digital Products, and Browse Services) are moved to the bottom of the left content column on desktop.
- Their vertical position now aligns with the bottom Messenger checkout card in the right column.
- Mobile behavior is unchanged.
- No client text, cards, navigation, filters, order data, admin page, JavaScript, pricing, or service logic was changed.

DEPLOYMENT STATUS
- This package applies the change locally only.
- It does not commit or push to GitHub.
- Run the read-only terminal audit and complete the visual check before publishing.
============================================================
V6.0.14 HERO WORDING AND ALIGNMENT
============================================================

Requested-only client homepage change:

1. Updated the hero headline to:
   The easiest way to browse, prepare, and process your
   Novalyte order.

2. Updated the supporting text to:
   Check services, digital subscriptions, and order details
   anytime. Message us for more information.

3. Moved the headline and supporting text slightly lower on
   desktop.

4. Preserved the three action buttons and the three right-side
   information cards.

5. No admin layout, data, filters, order tracker, navigation,
   pricing, or mobile behavior was intentionally changed.
============================================================
V6.0.15 DASHBOARD ANALYTICS AND MOBILE RECOMMEND SWITCH
============================================================

Requested-only update:

1. Removed the visible Admin dashboard quick-action controls:
   - Manage Services
   - Manage Digital Products
   - View Orders
   - Export Backup
   - Import Backup
   - Local browser mode

2. Preserved the underlying backup and cloud-status element IDs as
   hidden utility hooks so existing JavaScript event bindings remain
   compatible.

3. Added a presentable futuristic monthly Sales & Revenue Analytics
   card directly in the Welcome, Admin dashboard hero.

4. Analytics uses current order data and excludes cancelled and
   voided records.

5. Red chart line represents monthly client sales.

6. Blue chart line represents monthly service revenue. The dashboard
   also displays service ROI based on revenue versus provider cost.

7. Added monthly highest/lowest summaries for sales and revenue.

8. The chart rerenders whenever the existing Admin renderAll flow
   updates dashboard totals.

9. Preserved the Recommended control as the same custom toggle switch
   on desktop and mobile. Mobile changes only its responsive placement,
   not its component type or recommendation logic.

10. Disabled services retain the existing locked Recommend behavior.

11. No client page, order calculation, service price, digital product,
    investment, authentication, or Supabase logic was intentionally
    changed.
============================================================
V6.0.16 DASHBOARD ANALYTICS-ONLY CLEANUP
============================================================

Requested-only Admin Dashboard correction:

1. Removed the twelve legacy Dashboard summary cards from the
   visible Dashboard area.

2. Kept the Welcome, Admin heading and Sales & Revenue
   Analytics section visible.

3. Kept the five small analytics summary cards directly under
   the graph.

4. Preserved the hidden statsGrid hook so existing total and
   investment calculations continue to run safely.

5. The Investments tab, investment records, totals, Orders,
   Services, Digital Products, and other Admin sections were
   not removed or functionally changed.

6. No Git commit or push was performed by this local update.
============================================================
V6.0.17 LEGACY DASHBOARD CARDS HARD HIDE
============================================================

Corrective Admin Dashboard update:

1. Permanently hid the legacy statsGrid cards through a
   high-specificity display:none CSS rule.

2. Added runtime JavaScript enforcement so navigation, totals
   rendering, or later DOM updates cannot restore the cards.

3. Added a MutationObserver to preserve the hidden state if
   another function modifies the grid's class, style, or
   hidden attribute.

4. Preserved the Welcome, Admin section, Sales & Revenue
   Analytics chart, and its five compact summary cards.

5. Preserved all underlying calculations and the separate
   Investments tab.

6. No Git commit or push was performed by this local fix.
============================================================
V6.0.18 SERVICES TABLE ALIGNMENT
============================================================

Requested-only Admin Services correction:

1. Changed the desktop Services ledger to a fixed table layout
   so the configured column percentages are followed.

2. Reduced the actual horizontal space occupied by Recommend
   to only the space needed for its label and 42px switch.

3. Rebalanced the remaining columns without removing or
   changing any service data or action.

4. Increased the Platform column space and centered the social
   icon and platform name inside the column.

5. Added separation between Platform and Provider Rate so
   their contents no longer appear attached.

6. Preserved the existing Recommend switch behavior:
   - checked means recommended
   - unchecked means not recommended
   - disabled services keep the switch locked

7. Updated the Admin stylesheet cache query to v6.0.18.

8. No unrelated Client, Dashboard, Digital Product,
   Investment, Order, pricing, or data logic was changed.
============================================================
V6.0.19 PLAIN CLIENT LABELS
============================================================

Requested-only visual cleanup:

1. Removed the pill/container styling surrounding the
   "Digital growth solutions" text on the client homepage.

2. Preserved the text and its original location while changing
   it to a simple plain-text label.

3. Removed the rounded container, border, background, and
   focus box from the Admin sidebar "Client View" link.

4. Preserved Client View navigation and added a subtle text
   underline for hover and keyboard focus.

5. Updated both client and Admin stylesheet cache versions to
   v6.0.19.

6. No other homepage, Admin navigation, service table, data,
   order, investment, or responsive behavior was changed.
